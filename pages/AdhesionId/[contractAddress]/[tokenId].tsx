import { Default } from 'components/layouts/Default';
import TokenId from 'components/containers/adhesionId/[tokenId]';

const TokenIdPage = () => {
  return (
    <Default pageName="adhesion">
      <TokenId />
    </Default>
  );
};

export default TokenIdPage;
