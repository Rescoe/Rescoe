import { Default } from 'components/layouts/Default';
import { Formations } from 'components/containers/association/Formations';

const FormationsPage = () => {
  return (
    <Default pageName="Formations">
      <Formations />
    </Default>
  );
};

export default FormationsPage;
