import { Default } from 'components/layouts/Default';
import { Contact } from 'components/containers/association/Contact';

const ContactPage = () => {
  return (
    <Default pageName="Contact">
      <Contact />
    </Default>
  );
};

export default ContactPage;
